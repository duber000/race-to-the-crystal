# Production Secrets Template for Race to the Crystal
# DO NOT commit this file with actual secrets!
# Copy this template and fill in real values:
#   cp race-secrets.env.template ~/.config/containers/systemd/race-secrets.env
#   # Then generate real secrets below

# Mercure JWT Secrets (generate with: openssl rand -base64 32)
MERCURE_PUBLISHER_JWT=REPLACE_WITH_RANDOM_SECRET_32_BYTES
MERCURE_SUBSCRIBER_JWT=REPLACE_WITH_RANDOM_SECRET_32_BYTES

# Quick setup commands:
# PUBLISHER_JWT=$(openssl rand -base64 32)
# SUBSCRIBER_JWT=$(openssl rand -base64 32)
# cat > ~/.config/containers/systemd/race-secrets.env <<EOF
# MERCURE_PUBLISHER_JWT=$PUBLISHER_JWT
# MERCURE_SUBSCRIBER_JWT=$SUBSCRIBER_JWT
# EOF
# chmod 600 ~/.config/containers/systemd/race-secrets.env

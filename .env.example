# Race to the Crystal - Environment Configuration Example
# Copy this file to .env and customize the values

# =============================================================================
# Server Configuration
# =============================================================================

# HTTP/WebSocket port for all clients (web, desktop, AI)
HTTP_PORT=8080

# Server host binding (use 0.0.0.0 for Docker)
SERVER_HOST=0.0.0.0

# =============================================================================
# Mercure Configuration (Optional - for Server-Sent Events)
# =============================================================================

# Mercure hub URL
# For Docker: http://mercure:3000/.well-known/mercure
# For local: http://localhost:3000/.well-known/mercure
MERCURE_HUB_URL=http://localhost:3000/.well-known/mercure

# JWT token for publishing to Mercure hub. Leave empty to disable Mercure
# publishing (the server silently no-ops, and web clients use WebSocket).
# Generate with payload {"mercure":{"publish":["*"]}}, e.g.:
#   MERCURE_PUBLISHER_JWT=$(echo -n '{"mercure":{"publish":["*"]}}' | <sign-with-your-secret>)
# The signing secret must match MERCURE_PUBLISHER_JWT_KEY in the Mercure hub.
# DO NOT commit a real token here.
MERCURE_PUBLISHER_JWT=

# Topic prefix for game state updates
MERCURE_TOPIC_PREFIX=https://api.game.com/game

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# Game Configuration (Future - not yet implemented)
# =============================================================================

# Maximum players per game
# MAX_PLAYERS=4

# Game timeout in seconds
# GAME_TIMEOUT=3600

# AI difficulty level
# AI_DIFFICULTY=medium
